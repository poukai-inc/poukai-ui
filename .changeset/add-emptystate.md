---
"@poukai-inc/ui": minor
---

feat(molecule): add EmptyState — zero-data placeholder with optional icon, required title, optional description, and optional action slot. Centered column layout, two tones (default/subtle), token-only CSS, full a11y compliance.
