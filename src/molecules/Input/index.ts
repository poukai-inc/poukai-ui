/**
 * @deprecated `Input` has been promoted to the atom layer.
 * Import from `@poukai-inc/ui/atoms/Input` (or the root `@poukai-inc/ui`) instead.
 * This barrel will be removed in 2.0.
 *
 * Migration:
 * ```diff
 * - import { Input } from "@poukai-inc/ui/molecules/Input";
 * + import { Input } from "@poukai-inc/ui/atoms/Input";
 * ```
 */
export { Input, type InputProps, type InputSize } from "./Input";
