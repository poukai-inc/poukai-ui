---
"@poukai-inc/ui": minor
---

Add `Portrait` molecule — editorial photography primitive with AVIF/WebP/JPEG `<picture>` fallback chain, srcset generation, CLS-safe aspect-ratio, and enforced non-empty alt contract (WCAG 1.1.1).
